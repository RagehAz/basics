# Performance Optimization TODO

Scan date: 2026-09-12. Generated by scanning `lib/**` for perf anti-patterns (rebuild churn, hot-path allocation, unindexed queries, O(n²) loops, redundant recomputation, JSON round-trips, busy-waits). Recent fixes already landed (ZGridScale recomputation, video-pause-on-background, FireQueryModel limit, sembast→ObjectBox migration, IHLMOO hoist/busy-poll DB init) are **not** re-listed below — only new/remaining findings.

Work through these one at a time, top to bottom within each section (sections are ordered roughly by expected impact). Check items off as they're fixed. Each item: `file:line` — issue → fix — **hotness**.

---

## 1. Data layer (ObjectBox / filing / search) — highest leverage, touches every consumer

- [x] `lib/ldbob/src/models/av_bob.dart:61` (used at 302, 335-338, 373-377, 511, 536, 564-567) — `AvBob.bobDocName` has no `@Index()`, yet every query against the shared box (`readAll`, `deleteAll`, `countItemsInBob`, `findBobByModelID`, `findBobsByModelsIDs`, `checkExistsByModelID`) filters on it across 7 docNames sharing one box → add `@Index()` to `bobDocName`. **hotness: high** — DONE: added `@Index()`, regenerated `objectbox.g.dart`/`objectbox-model.json` via `dart run build_runner build`, verified with `flutter analyze` + `flutter test` (no new failures).
- [x] `lib/filing/src/file/director.dart:157-197` (`readDirectoryFilesPaths`), `:326-367` (`findFilePathByName`), fanned out from `filers.dart:422-439,775-801` and `x_filers.dart:20,393-410,664-681` — every single-file lookup (`checkFileExistsByName`, `readByName`, `getOrCreateTempFile`) does a fresh recursive `Directory.listSync()` scan instead of using the existing path cache in `directory_operator.dart:17-19` → route through `DirectoryOperator.getPaths()` + `File.exists()` instead of rescanning disk. **hotness: high** — DONE: `findFilePathByName` now reuses `DirectoryOperator.getPaths()` when `directoryType == DirectoryType.app` (the case every real caller uses), falling back to a fresh scan only for other directory types. Verified with `flutter analyze` + `flutter test` (no new failures).
- [ ] `lib/filing/src/file/director.dart:177,264` — `Directory(...).listSync(recursive: true)` blocks the isolate with sync I/O inside `async` methods → use `Directory.list()` stream (`await for`) instead. **hotness: high**
- [ ] `lib/ldb/src/ops/ldb_search.dart` (`byFinder`, `firstWithValue`, `anyWithValue`, `anyInList`, `fromPhrases`) backed by `ldb_bob.dart:300-329` (`readAll`) — every search loads + JSON-decodes *every* record for a docName then filters/sorts in Dart, instead of building an ObjectBox `Condition` directly for `Filter.equals`/`Filter.inList` like `LdbBobOps.readMaps` already does → push common filter cases down into ObjectBox conditions. **hotness: high**
- [ ] `lib/ldbob/src/models/ldb_bob.dart:177-195` (`insertMany`, `allowDuplicateIDs == false`, the default) — N+1 pattern: loops and calls `insert()` per item, each running its own existence query before `put` → batch with one `LdbBob_.recordID.oneOf(...)` lookup up front, then a single `putMany`. **hotness: medium-high**
- [ ] `lib/helpers/strings/stringer.dart:77-102,105-130` — `addStringToListIfDoesNotContainIt`/`addStringsToStringsIfDoNotContainThem` copy the whole accumulator list and linear-scan with `.contains` per item → O(n²); feeds `TextFind.urls/emails/phones/hashtags` (parses all user-generated text) → dedupe via `LinkedHashSet<String>` in one O(n) pass. **hotness: high**
- [ ] `lib/helpers/maps/mapper.dart:66` (`getMapsFromDynamics`) — does `jsonDecode(jsonEncode(object))` per list element to coerce to `Map<String,dynamic>`, same anti-pattern already fixed for IHLMOO → reuse `_deepConvertIHLMOOValue` (line 154) per element. **hotness: high**
- [ ] `lib/helpers/maps/mapper.dart:1328` (`cloneMap`) — still deep-copies via `jsonDecode(jsonEncode(map))` → implement via `_deepConvertIHLMOOValue`/manual recursive copy. **hotness: medium**
- [ ] `lib/filing/src/floaters/byter.dart:15-28,101-114,238-260` (`fromAvModels`, `fromFiles`, `fromLocalAssets`) — sequential `await` per item serializes I/O latency → parallelize with `Future.wait(list.map(...))`. **hotness: medium**
- [ ] `lib/filing/src/file/directory_operator.dart:56-59,80-83,106-110` — `addPath`/`removePath`/`checkExists` linear-scan the whole cached path list per call → switch to `Set`/`Map` lookup (do this alongside the `director.dart` fix above, which is what currently forces bypassing this cache). **hotness: medium**
- [ ] `lib/ldb/src/foundation/sembast_*.dart` (all 7 files) — dead code post ObjectBox migration, nothing references it except one commented-out line in `bob_info.dart:10` → delete to avoid accidental re-wiring. **hotness: none (cleanup only)**

## 2. String/number/collection helpers (`lib/helpers/**`) — called across the entire consuming app

- [ ] `lib/helpers/nums/numeric.dart:14-102` (`formatNumToSeparatedKilos`) — computes a full fraction-aware grouping result (lines 46-77) that is then unconditionally overwritten by a second recomputation (82-93) → delete the dead first computation, halves the work per call. **hotness: high**
- [ ] `lib/helpers/strings/text_find.dart:26-32` (`getStringsByRegEx`, used by `urls()`/`emails()`) — builds a fresh `RegExp` from a literal pattern every call, unlike the file's own hoisted `_hashtagWithDashExp` pattern → hoist URL/email patterns to `static final RegExp`. **hotness: high**
- [ ] `lib/helpers/nums/numeric.dart:125,138` (`formatNumToCounterCaliber`) — constructs `RegExp('0.0')` fresh on every call, twice → hoist to a `static final` field. **hotness: medium**
- [ ] `lib/helpers/strings/text_mod.dart:654-700,708-716` (`fixCountryName`) — rebuilds a ~38-entry map literal every call and does 38 sequential `.replaceAll()` full-string passes → hoist base map to `static const`, replace with a single-pass translation/RegExp. **hotness: medium**
- [ ] `lib/helpers/strings/text_mod.dart:423-435` (`removeSpacesFromAString`) — 5 chained `.replaceAll()` passes to strip space variants → single pass with one `RegExp` alternation. **hotness: medium**
- [ ] `lib/helpers/time/timers.dart:622-624` (`createClock`) — calls `DateTime.now()` three separate times for year/month/day (also a correctness risk on rollover) → capture once, reuse. **hotness: medium**
- [ ] `lib/helpers/checks/object_check.dart:24-54` (`isAbsoluteURL`) — wraps a sync `Uri.parse` check in the async `tryAndCatch` helper, allocating a `Future`/closure for sync work → plain `try/catch`. **hotness: medium**
- [ ] `lib/helpers/maps/lister.dart:229-250` (`fillEmptySlotsUntilIndex`) — uses `removeAt`+`insert` per element (shifts trailing elements) making it O(n²) → direct `_output[i] = list[i]` assignment. **hotness: low**
- [ ] `lib/helpers/strings/stringer.dart:305-332` (`sortAlphabetically`) — comparator calls `.toLowerCase()` on both sides every comparison during sort → decorate-sort-undecorate (lowercase once per element up front). **hotness: low**
- [ ] `lib/helpers/checks/object_check.dart:201-222` (`objectIsJPGorPNG`) — calls `getExtensionFromPath` three times for the same input → compute once into a local. **hotness: low**
- [ ] `lib/helpers/pixels/pixelizer.dart:67-108` (`getPixelColor`) — wraps a sync ARGB calc in async `tryAndCatch`, runs per pointer-move during pixel-picking → plain sync `try/catch`. **hotness: low**

## 3. Widget rebuild churn (`lib/components/**`, `lib/circle_list/**`, `lib/pixel_color_picker/**`)

- [ ] `lib/circle_list/circle_list.dart:206-222` — initial-animation controller listener calls `setState` on every tick (up to 60/sec) even after the guarded bool has already flipped → guard with `if (isAnimationStop != false)`, or drive rotation via `AnimatedBuilder` instead of `setState`. Same bug class as the already-fixed ZGridScale issue. **hotness: high**
- [ ] `lib/circle_list/circle_list.dart:337-361` — every drag-update `setState`/animation tick re-runs `List.generate` recomputing per-child trig + allocating fresh widgets for the whole ring, even though only rotation changed → hoist per-child radius/offset math out of the angle-dependent path, wrap children in their own `RepaintBoundary`. **hotness: high**
- [ ] `lib/components/animators/auto_scrolling_bar.dart:72-83` — `_scrollListener` allocates a new `Map<String,dynamic>` on every scroll-position callback and pushes to a `ValueNotifier` regardless of whether direction actually changed → compare `ScrollDirection` before notifying, or use two primitive fields instead of a re-allocated map. **hotness: high**
- [ ] `lib/components/layers/tap_layer/src/tap_ink_layer.dart:31-34`, `tap_state_with_ups_and_downs.dart:59-60` — allocates new closures wrapping callbacks on every `build()`; this is the base of every tappable widget in the package → pass callbacks straight through instead of wrapping in fresh lambdas each build. **hotness: medium (very high multiplier — most-instantiated widget family)**
- [ ] `lib/components/super_slider/src/components/tight_track_shape.dart:39-42,95` — `paint()` allocates new `ColorTween`/`Paint` objects every frame during drag → cache and reuse across paints when colors haven't changed. **hotness: medium**
- [ ] `lib/pixel_color_picker/dart/src/controller/pixel_color_controller.dart:188-201`, `a_pixel_picker.dart:85-97` — `onPanUpdate` fires unthrottled async pixel reads per pointer-move, can race/queue out of order on fast drags → skip/debounce when a previous call is still in flight. **hotness: medium**
- [ ] `lib/components/zoomers/zoomer/src/new_zoomer/new_zoomable_child.dart:458-465` — `onInteractionUpdate` calls `entry?.markNeedsBuild()` on every gesture-update event regardless of scale delta size → skip rebuild when `(details.scale - scale).abs()` is below an epsilon. **hotness: medium**
- [ ] `lib/components/zoomers/zoomer/src/old_zoomer/old_zoomer.dart` + `new_zoomer/new_zoomable_child.dart` — ~190 lines of dead/commented-out duplicate `_ZoomableChild` implementation bloats compile/analyze time → delete. **hotness: none (cleanup only)**

## 4. Grid/layout/video (`lib/z_grid/**`, `lib/layouts/**`, `lib/av/**`, `lib/models/**`)

- [ ] `lib/av/src/e_av_playing/super_video_player/src/players/d_file_player.dart:24-129` — entire video widget tree (card, cover, loading, play/error icons, volume slider) sits inside one `SingleWire<VideoPlayerValue?>` builder, rebuilding all of it on every player tick (many/sec during playback) → split into smaller listenables/`AnimatedBuilder`s scoped to what actually depends on each changed field. **hotness: high**
- [ ] `lib/av/src/e_av_playing/super_video_player/src/players/d_file_player.dart:65-66` — recomputes `Idifier.idifyString(fileName)` for a `ValueKey` on every tick even though the file name is constant during playback → compute once (initState/on load). **hotness: high**
- [ ] `lib/av/src/e_av_playing/super_video_player/src/players/d_file_player.dart:28-40` + `controllers/super_video_scale.dart:56-125` — `getHeightOnScreen`/`getWidthOnScreen` each independently call `getDimsOnScreen`/`Dimensions.fitGraphicToCanvas` with identical inputs, doubling the fit computation per tick — same class of bug as the already-fixed ZGridScale recomputation, not carried through here → call `getDimsOnScreen` once, read `.width`/`.height` off one result. **hotness: high**
- [ ] `lib/av/src/a_models/av_model/av_model.dart:649-665` (`checkModelsAreIdentical`) via `av_cipher.dart:10-40` (`AvCipher.toMap`) — every `AvModel ==` comparison allocates two 18-key maps and diffs them instead of direct field comparison (unlike `hashCode` and `Flag.checkFlagsAreIdentical`, which do it cheaply) → rewrite as direct field comparisons. Runs on every rebuild of anything comparing two `AvModel`s (e.g. image widgets' `didUpdateWidget`). **hotness: medium-high**
- [ ] `lib/av/src/a_models/av_model/av_model.dart:649-665` — unconditionally calls `blogDifferences` (a second full field walk + string-interpolation allocations for logging) whenever two models differ, on the hot equality path itself → gate behind a debug flag or move out of the equality operator. **hotness: medium**
- [ ] `lib/z_grid/src/z_grid_scale.dart:698-746` (`_getZoomMatrix`) — calls `calculateMaxScale` directly and again indirectly through `_getHorizontalZoomOffset`/`_getTopPaddingZoomOffset`, tripling the `MediaQuery`-dependent chain per zoom gesture → compute once, pass down. Same unaddressed pattern as the already-fixed ZGridScale issue, different call chain. **hotness: medium**
- [ ] `lib/z_grid/src/z_grid_scale.dart:325-372` (`getGridPadding`), `:444-479` (`getGridDelegate`) — each recomputed on every grid rebuild and each internally recomputes `getSmallItemWidth`/`getGridSideMargin` more than once → thread one precomputed value through, mirroring the ZGridScale.initialize fix. **hotness: medium**
- [ ] `lib/av/src/e_av_playing/super_video_player/src/controllers/super_video_checkers.dart:16,29` (`checkIsValidYoutubeVideoID`/`checkIsValidYoutubeLink`) — compiles a new `RegExp` per call, invoked on every video URL load → hoist to `static final RegExp`. **hotness: low-medium**
- [ ] `lib/layouts/handlers/max_bounce_navigator.dart:81-83` — `build()` calls `Scale.screenHeight(context)` twice for the same value on every rebuild → compute once into a local. **hotness: low**

## 5. Notes / informational (verify, no action confirmed needed yet)

- [ ] Verify `Wire.set`/`LiveWire` (in `lib/helpers/wire`) short-circuits on equal values — several sensor widgets (`FingersSensor`, `ScrollSensor` in `lib/components/sensors/`) push every pointer/scroll event through it without an obvious equality check, and it's exercised on a hot per-pointer-event path.
- [ ] `lib/components/super_box/src/a_the_box_of_super_box/x_the_box_of_super_box.dart:64-96` — `SuperBox` builds always allocate a fresh `Row`+`IntrinsicWidth` wrapper even for static buttons; low value alone but `SuperBox` is extremely widely instantiated — only worth it if profiling shows it matters.

---

**Areas checked and found clean** (no action items): `lib/exports/**`, `lib/bldrs_theme/**`, `lib/components/super_image/super_filter/**`, `lib/components/texting/super_text_field/**`, `lib/components/bubble/**`, `lib/components/drawing/**`, `lib/components/dialogs/**`, `LdbBobOps` (already properly indexed/queried), `FlyerBob`/`BzBob`/`FishBob`/`UserBob` (`getAll()` there is correct — each has its own dedicated box), `BobInit.getStoreRecursive` (proper `Completer`-based init, no polling).
